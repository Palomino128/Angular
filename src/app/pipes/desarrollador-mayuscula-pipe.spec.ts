  import { DesarrolladorMayusculaPipe } from './desarrollador-mayuscula-pipe';

describe('DesarrolladorMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new DesarrolladorMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
