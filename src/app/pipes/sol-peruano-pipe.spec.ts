import { SolPeruanoPipe } from './sol-peruano-pipe';

describe('SolPeruanoPipe', () => {
  it('create an instance', () => {
    const pipe = new SolPeruanoPipe();
    expect(pipe).toBeTruthy();
  });
});
